package com.ashok.springwebflux.services;

import org.springframework.stereotype.Service;

//import com.ashok.springwebflux.model.CalculatorDTO;

import reactor.core.publisher.Mono;

@Service
public class WebfluxService {

//  public Mono<CalculatorDTO> findSum(CalculatorDTO calculatorDTO) {
//    CalculatorDTO responseDTO = CalculatorDTO.builder().argC(calculatorDTO.getArgA() + calculatorDTO.getArgB()).build();
//    return Mono.just(responseDTO);
//  }
}
